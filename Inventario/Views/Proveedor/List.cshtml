@model ProveedorViewModel


<h4 class="header">Lista de proveedores</h4>

<div class="panel panel-default col s12">
    <!-- Default panel contents -->
    <div class="panel-heading">Panel de clientes</div>
    <table class="table highlight">
        <thead>
            <tr>
                <th>
                    Nombre
                </th>
                <th>
                    Apellido
                </th>
                <th>
                    Cedula
                </th>
                <th>
                    Telefono
                </th>
                <th>
                    Direccion
                </th>
                <th>
                    Email
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Proveedores)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdentificationCard)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PhoneNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Address)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ProveedorId" class="btn-floating btn-small waves-effect waves-light #26a69a"><i class="material-icons">edit</i></a>

                    </td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@item.ProveedorId" class="btn-floating btn-small waves-effect waves-light red"><i class="material-icons">delete</i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <br />
</div>

<p>
    <a asp-action="Create" class="btn-floating btn-small waves-effect waves-light blue"><i class="material-icons">person_add</i></a>
</p>
